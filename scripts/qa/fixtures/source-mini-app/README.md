# Source Mini App

Small TypeScript fixture for `/onlymacs` code-review QA.

Known rough edges:

- auth token handling is intentionally weak
- network error handling is intentionally thin
- tests are intentionally missing
